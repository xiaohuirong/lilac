from lilaclib import *

def pre_build():
    #aur_pkgver, aur_pkgrel = get_pkgver_and_pkgrel()
    aur_pre_build()
    #for line in edit_file('PKGBUILD'):
    #    if line.startswith('pkgrel='):
    #        print(f'aur_pkgrel={aur_pkgrel}')
    #    print(line)
    

