maintainers:
  - github: Hoream
    email: hoream@qq.com

build_prefix: extra-x86_64
pre_build_script: |
  aur_pre_build()
  #  update_pkgver_and_pkgrel(_G.newver)

post_build: git_pkgbuild_commit

update_on:
  - source: aur
    aur: spacedrive-bin
      #strip_release: true
